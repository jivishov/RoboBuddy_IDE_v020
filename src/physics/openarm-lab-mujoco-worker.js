import { OpenArmServo, OPENARM_CONTROL_PROFILE } from './openarm-servo.js';
import { solveOpenArmIK } from './openarm-ik.js';
import { stripOpenArmReferenceWorkcellXml } from './openarm-lab-builder.js';
import { validateOpenArmLabEquipment, appendOpenArmLabEquipmentXml } from './openarm-lab-equipment.js';
import loadMujoco from '../../assets/microduck/runtime/mujoco/mujoco.js';
import { MAX_ADVANCE_STEPS_PER_REQUEST, MAX_SAMPLED_OBSERVATIONS_PER_ADVANCE, sampledObservationCount } from './backend-contract.js';

const MUJOCO_BASE_URL = new URL('../../assets/microduck/runtime/mujoco/', import.meta.url);
const EXPECTED_MUJOCO_VERSION = '3.11.0';
const MODEL_ASSET_RE = /^models\/[A-Za-z0-9._/-]+\.xml$/;
const INTEGRATOR_CODES = Object.freeze({ Euler:0, RK4:1, implicit:2, implicitfast:3 });
let mujoco=null, model=null, data=null, descriptor=null, modelInfo=null, servo=null, equipment=null, motionPlan=null, paused=false;
let jointState=new Map(), couplingState=new Map(), actuatorState=new Map(), bodyState=new Map();
const reply=(id,ok,payload=null,error=null)=>postMessage({id,ok,payload,error});
async function ensureMuJoCo(){ if(!mujoco)mujoco=await loadMujoco({locateFile:path=>new URL(path,MUJOCO_BASE_URL).href}); return mujoco; }
async function sha256Text(text){ const digest=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(text)); return [...new Uint8Array(digest)].map(v=>v.toString(16).padStart(2,'0')).join(''); }
function enumValue(name){ const entry=mujoco?.mjtObj?.[name], value=entry&&typeof entry==='object'&&'value'in entry?entry.value:entry; if(!Number.isInteger(Number(value)))throw new Error(`MuJoCo object enum ${name} unavailable`); return Number(value); }
function idFor(type,name){ const id=Number(mujoco.mj_name2id(model,enumValue(type),name)); if(!Number.isInteger(id)||id<0)throw new Error(`Required MuJoCo ${type} ${name} missing`); return id; }
function nameForGeom(id){ if(!Number.isInteger(id)||id<0||typeof mujoco?.mj_id2name!=='function')return null; try{return mujoco.mj_id2name(model,enumValue('mjOBJ_GEOM'),id)||null;}catch{return null;} }
function validatedModelUrl(asset){ if(!MODEL_ASSET_RE.test(asset||'')||String(asset).includes('..'))throw new Error('Lab Builder rejected model path'); const url=new URL(`../../${asset}`,import.meta.url); if(url.origin!==self.location.origin)throw new Error('Model asset must be same-origin'); return url.href; }
function closeArray(actual,expected,label,tol=1e-9){ if(!Array.isArray(expected)||actual.length!==expected.length)throw new Error(`${label} shape mismatch`); actual.forEach((v,i)=>{if(!Number.isFinite(v)||Math.abs(v-Number(expected[i]))>tol)throw new Error(`${label} mismatch at ${i}`);}); }
function runtimeVersion(){ const version=typeof mujoco?.mj_versionString==='function'?String(mujoco.mj_versionString()):EXPECTED_MUJOCO_VERSION; if(version!==EXPECTED_MUJOCO_VERSION)throw new Error(`Bundled MuJoCo ${version}; expected ${EXPECTED_MUJOCO_VERSION}`); return version; }

function resolveAddresses(modelSha256){
  jointState=new Map(); couplingState=new Map(); actuatorState=new Map(); bodyState=new Map();
  for(const joint of descriptor.joints){ const id=idFor('mjOBJ_JOINT',joint.id), qpos=Number(model.jnt_qposadr[id]), dof=Number(model.jnt_dofadr[id]), range=[Number(model.jnt_range[id*2]),Number(model.jnt_range[id*2+1])], axis=[Number(model.jnt_axis[id*3]),Number(model.jnt_axis[id*3+1]),Number(model.jnt_axis[id*3+2])]; if(joint.rangeRad)closeArray(range,joint.rangeRad,`${joint.id} range`); if(joint.axis)closeArray(axis,joint.axis,`${joint.id} axis`); jointState.set(joint.id,{id,qpos,dof,range,axis}); }
  for(const coupling of descriptor.mechanicalCouplings||[]){ const driver=jointState.get(coupling.driverJointId); if(!driver)throw new Error(`Unknown coupling driver ${coupling.driverJointId}`); const id=idFor('mjOBJ_JOINT',coupling.followerJointId), qpos=Number(model.jnt_qposadr[id]), dof=Number(model.jnt_dofadr[id]), range=[Number(model.jnt_range[id*2]),Number(model.jnt_range[id*2+1])], axis=[Number(model.jnt_axis[id*3]),Number(model.jnt_axis[id*3+1]),Number(model.jnt_axis[id*3+2])]; couplingState.set(coupling.id,{...coupling,driver,id,qpos,dof,range,axis}); jointState.set(coupling.followerJointId,{id,qpos,dof,range,axis,passiveCoupled:true}); }
  for(const actuator of descriptor.actuators){ const id=idFor('mjOBJ_ACTUATOR',actuator.id), joint=jointState.get(actuator.jointId); if(!joint)throw new Error(`Unknown actuator joint ${actuator.jointId}`); const controlRange=[Number(model.actuator_ctrlrange[id*2]),Number(model.actuator_ctrlrange[id*2+1])]; if(actuator.controlRangeRad)closeArray(controlRange,actuator.controlRangeRad,`${actuator.id} control range`); actuatorState.set(actuator.id,{...actuator,id,controlRange}); }
  for(const body of descriptor.bodies||[]){ const id=idFor('mjOBJ_BODY',body.id); let freeDof=null; if(body.freeJointId){ const j=idFor('mjOBJ_JOINT',body.freeJointId); freeDof=Number(model.jnt_dofadr[j]); } bodyState.set(body.id,{id,freeDof}); }
  const timestepSeconds=Number(model.opt?.timestep), expected=Number(descriptor.physics.timestepSeconds); if(!Number.isFinite(timestepSeconds)||Math.abs(timestepSeconds-expected)>1e-12)throw new Error(`Unexpected timestep ${timestepSeconds}`);
  if(Number(model.opt?.integrator)!==INTEGRATOR_CODES[descriptor.physics.integrator])throw new Error('Unexpected integrator');
  modelInfo={modelSha256,engineVersion:runtimeVersion(),timestepSeconds};
}
function positionActuator(jointId,required=false){ const matches=[...actuatorState.values()].filter(a=>a.jointId===jointId&&a.command==='position-rad'); if(matches.length>1||(required&&matches.length!==1))throw new Error(`Joint ${jointId} has invalid actuator mapping`); return matches[0]||null; }
function contacts(){ const count=Number(data?.ncon||0); if(count>512)throw new RangeError('Lab Builder contact budget exceeded'); const result=[]; if(!data?.contact)return{count,readable:count===0,contacts:result}; const force=new mujoco.DoubleBuffer(6), collection=data.contact; let readable=true; try{ const available=typeof collection.size==='function'?Number(collection.size()):count; if(available<count)readable=false; for(let i=0;i<Math.min(count,available);i++){ const c=typeof collection.get==='function'?collection.get(i):collection[i]; if(!c){readable=false;continue;} try{ const geom1=Number(c.geom?.[0]??c.geom1??-1), geom2=Number(c.geom?.[1]??c.geom2??-1); mujoco.mj_contactForce(model,data,i,force); result.push({geom1,geom2,geom1Name:nameForGeom(geom1),geom2Name:nameForGeom(geom2),distanceM:Number(c.dist??0),normalForceN:Math.max(0,Number(force.GetView()[0])),positionM:Array.from(c.pos||[]).slice(0,3)}); }finally{c.delete?.();} } }finally{force.delete();collection.delete?.();} return{count,readable,contacts:result}; }
function observation(){
  if(!model||!data)return{simulationTime:0,joints:{},bodies:{},contactCount:0,contactsReadable:false,contacts:[]};
  const joints={}; for(const [name,joint] of jointState){ const actuator=positionActuator(name); joints[name]={positionRad:Number(data.qpos[joint.qpos]),velocityRadS:Number(data.qvel[joint.dof]),targetRad:actuator?servo?.target(name)??Number(data.ctrl[actuator.id]):null,referenceRad:actuator?servo?.reference(name)??Number(data.ctrl[actuator.id]):null,actuatorTargetRad:actuator?Number(data.ctrl[actuator.id]):null,effortNm:actuator?Number(data.actuator_force?.[actuator.id]??0):null,controlRangeRad:actuator?[...actuator.controlRange]:null,jointRangeRad:[...joint.range]}; }
  const bodies={}; for(const [name,body] of bodyState){ const p=body.id*3,q=body.id*4,record={frame:'mujoco_world',positionM:[Number(data.xpos[p]),Number(data.xpos[p+1]),Number(data.xpos[p+2])],quaternionWxyz:[Number(data.xquat[q]),Number(data.xquat[q+1]),Number(data.xquat[q+2]),Number(data.xquat[q+3])]}; if(body.freeDof!=null){record.linearVelocityFrame='mujoco_world';record.angularVelocityFrame='body_local';record.linearVelocityMS=[0,1,2].map(i=>Number(data.qvel[body.freeDof+i]));record.angularVelocityRadS=[3,4,5].map(i=>Number(data.qvel[body.freeDof+i]));} bodies[name]=record; }
  const c=contacts(); const pinchReferences={}; for(const side of ['left','right']){ const site=idFor('mjOBJ_SITE',`${side}_pinch_reference`), ee=bodies[`openarm_${side}_ee_base_link`]; pinchReferences[side]={frame:'mujoco_world',positionM:Array.from(data.site_xpos.slice(site*3,site*3+3)),quaternionWxyz:ee.quaternionWxyz}; }
  return{simulationTime:Number(data.time||0),model:{id:descriptor.modelId||descriptor.id,asset:descriptor.asset,sha256:modelInfo.modelSha256},engine:{version:modelInfo.engineVersion,timestepSeconds:modelInfo.timestepSeconds},joints,bodies,contactCount:c.count,contactsReadable:c.readable,contacts:c.contacts,openarm:{workspaceMode:'lab_builder',pinchReferences,observationPhase:'post-step forward dynamics',controlProfile:OPENARM_CONTROL_PROFILE,motionPlan,equipment:equipment?.records||[],equipmentJoints:[]},setupLog:descriptor.equipment?.length?[{type:'explicit-lab-builder-compile-and-reset',simulationTimeSeconds:0,equipmentIds:descriptor.equipment.map(e=>e.id),modelSha256:modelInfo.modelSha256}]:[]};
}
function applyInitial(){ mujoco.mj_resetData(model,data); for(const [jointId,raw] of Object.entries(descriptor.initialJointPositionsRad||{})){ const joint=jointState.get(jointId),value=Number(raw); if(!joint||!Number.isFinite(value))throw new Error(`Invalid initial joint ${jointId}`); const actuator=positionActuator(jointId); data.qpos[joint.qpos]=value; if(actuator)data.ctrl[actuator.id]=value; } for(const coupling of couplingState.values()){ const value=Number(data.qpos[coupling.driver.qpos])*Number(coupling.multiplier??1)+Number(coupling.offsetRad??0); data.qpos[coupling.qpos]=value; } paused=false; mujoco.mj_forward(model,data); servo=new OpenArmServo(model,data,actuatorState,jointState); servo.apply(); mujoco.mj_forward(model,data); motionPlan=null; return observation(); }
function dispose(){ try{data?.delete?.();}catch{} try{model?.delete?.();}catch{} data=model=descriptor=modelInfo=servo=equipment=motionPlan=null; jointState=new Map();couplingState=new Map();actuatorState=new Map();bodyState=new Map();paused=false; }
function validateClearance(){ if(!equipment)return; const names=Array.from({length:Number(model.ngeom)},(_,i)=>nameForGeom(i)); const owner=name=>equipment.records.find(e=>e.geometryIds.includes(name))?.id; for(let a=0;a<names.length;a++){const ownA=owner(names[a]);if(!ownA)continue;for(let b=0;b<names.length;b++){if(a===b||owner(names[b])===ownA||(owner(names[b])&&b<a))continue;const distance=Number(mujoco.mj_geomDistance(model,data,a,b,.001,new Float64Array(6)));if(distance<-.0005)throw new RangeError(`Initial asset overlap ${names[a]} / ${names[b]} by ${(-distance*1000).toFixed(2)} mm`);}} }
async function load(modelPackage){ if(!modelPackage?.labBuilder||!modelPackage?.asset||!modelPackage?.baseSha256||!modelPackage?.sha256)throw new Error('Lab Builder requires a registered labBuilder model package'); const url=validatedModelUrl(modelPackage.asset),mj=await ensureMuJoCo(); dispose(); let xml=await fetch(url,{cache:'no-store'}).then(r=>{if(!r.ok)throw new Error(`Model HTTP ${r.status}`);return r.text();}); if(await sha256Text(xml)!==modelPackage.baseSha256)throw new Error('Base OpenArm model checksum mismatch'); xml=stripOpenArmReferenceWorkcellXml(xml); if(modelPackage.equipment?.length){ const normalized=validateOpenArmLabEquipment(modelPackage.equipment); equipment=appendOpenArmLabEquipmentXml(xml,normalized); xml=equipment.xml; } if(await sha256Text(xml)!==modelPackage.sha256)throw new Error('Compiled Lab Builder model checksum mismatch'); model=mj.from_xml_string(xml); if(!model)throw new Error('MuJoCo failed to compile Lab Builder scene'); data=new mj.MjData(model); descriptor=structuredClone(modelPackage); resolveAddresses(modelPackage.sha256); const initial=applyInitial(); validateClearance(); return initial; }
function integrate(){ servo.apply();mujoco.mj_step(model,data);mujoco.mj_forward(model,data);if(!Array.from(data.qpos).every(Number.isFinite)||!Array.from(data.qvel).every(Number.isFinite))throw new Error('Non-finite Lab Builder state'); }
function step(count=1){ if(!Number.isInteger(count)||count<1||count>MAX_ADVANCE_STEPS_PER_REQUEST)throw new RangeError('Invalid step count');if(!paused)for(let i=0;i<count;i++)integrate();return observation(); }
function stepSampled(count=1,sampleEverySteps=1){ if(!Number.isInteger(count)||count<1||count>MAX_ADVANCE_STEPS_PER_REQUEST)throw new RangeError('Invalid step count');if(!Number.isInteger(sampleEverySteps)||sampleEverySteps<1)throw new RangeError('Invalid sample cadence');if(paused)return{observations:[observation()],executedSteps:0,sampleEverySteps};const expected=sampledObservationCount(count,sampleEverySteps);if(expected>MAX_SAMPLED_OBSERVATIONS_PER_ADVANCE)throw new RangeError('Observation sample budget exceeded');const observations=[];for(let i=1;i<=count;i++){integrate();if(i===count||i%sampleEverySteps===0)observations.push(observation());}return{observations,executedSteps:count,sampleEverySteps}; }
function validatedJointTarget(jointId,value){ const joint=jointState.get(jointId),actuator=positionActuator(jointId,true),target=Number(value);if(!joint||!Number.isFinite(target))throw new Error(`Invalid joint target ${jointId}`);if(target<actuator.controlRange[0]||target>actuator.controlRange[1])throw new RangeError(`${jointId} target outside actuator range`);if(target<joint.range[0]||target>joint.range[1])throw new RangeError(`${jointId} target outside joint range`);return target; }
function command(payload={}){ let targets;if(payload.type==='set_joint_targets'||payload.type==='move_joint_targets'){if(!payload.targetsRad||typeof payload.targetsRad!=='object'||Array.isArray(payload.targetsRad)||!Object.keys(payload.targetsRad).length)throw new Error('Joint targets required');targets=Object.fromEntries(Object.entries(payload.targetsRad).map(([id,value])=>[id,validatedJointTarget(id,value)]));const timing=servo.request(targets,payload.type==='move_joint_targets'?payload.durationSeconds:null);motionPlan={type:payload.type,...timing,collisionFreePath:false};return observation();}if(payload.type==='set_tool_target'){const solution=solveOpenArmIK(mujoco,model,data,jointState,payload);targets=Object.fromEntries(Object.entries(solution.targetsRad).map(([id,value])=>[id,validatedJointTarget(id,value)]));const timing=servo.request(targets,payload.durationSeconds??null);motionPlan={...solution,...timing,collisionFreePath:false,plannerScope:'IK target only; Lab Builder transfer planner validates authored task-space clearance separately'};return observation();}throw new Error(`Unsupported command ${payload.type}`); }
self.onmessage=async event=>{const{id,op,payload}=event.data||{};try{let result;if(op==='load')result=await load(payload?.modelPackage);else if(op==='reset')result=applyInitial();else if(op==='step')result=step(payload?.count);else if(op==='stepSampled')result=stepSampled(payload?.count,payload?.sampleEverySteps);else if(op==='observe')result=observation();else if(op==='command')result=command(payload);else if(op==='pause'){paused=true;result=observation();}else if(op==='resume'){paused=false;result=observation();}else if(op==='dispose'){dispose();result=true;}else throw new Error(`Unknown operation ${op}`);reply(id,true,result);}catch(error){reply(id,false,null,String(error?.stack||error?.message||error));}};
