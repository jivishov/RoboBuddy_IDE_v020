#!/usr/bin/env python3
from pathlib import Path
import base64, io, zipfile, json
ROOT=Path(__file__).resolve().parents[1]
PAYLOAD=''.join(p.read_text() for p in sorted((ROOT/'scripts').glob('phase5a.payload.*')))
PATCHES=json.loads('[["src/app-v2.js", "    const visibleDriver = id === \'so101\' ? \'robobuddy.sim.v1 \\u00b7 browser MuJoCo\' : p.driver;\\n    $(\'driverLabel\').textContent = visibleDriver;\\n    $(\'driverStatus\').textContent = visibleDriver;\\n    this.updateSimulationPresentation(p);\\n    this.updateExecutionControls();\\n    this.setStatus(id === \'so101\' ? \'Loading SO-101 MuJoCo physical workspace\\u2026\' : p.simulationMode === \'policy_sim\' ? \'Loading local MicroDuck runtime visual\\u2026\' : p.simulationMode === \'kinematic_pose\' ? \'Loading Unitree canonical pose workspace\\u2026\' : \'Loading reviewed mission and source plant\\u2026\');", "    const migratedPhysical = id === \'so101\' || id === \'openarm\';\\n    const visibleDriver = migratedPhysical ? \'robobuddy.sim.v1 \\u00b7 browser MuJoCo\' : p.driver;\\n    $(\'driverLabel\').textContent = visibleDriver;\\n    $(\'driverStatus\').textContent = visibleDriver;\\n    this.updateSimulationPresentation(p);\\n    this.updateExecutionControls();\\n    this.setStatus(migratedPhysical ? `Loading ${p.shortLabel} MuJoCo physical workspace\\u2026` : p.simulationMode === \'policy_sim\' ? \'Loading local MicroDuck runtime visual\\u2026\' : p.simulationMode === \'kinematic_pose\' ? \'Loading Unitree canonical pose workspace\\u2026\' : \'Loading reviewed mission and source plant\\u2026\');", 1], ["src/app-v2.js", "      this.problem(\'error\', id === \'so101\' ? \'PHYSICAL_WORKSPACE\' : p.simulationMode === \'policy_sim\' ? \'MICRODUCK_RIG\' : p.simulationMode === \'kinematic_pose\' ? \'RIG_WORKSPACE\' : \'SOURCE_TASK\', String(error.message || error));\\n      this.setStatus(id === \'so101\' ? \'SO-101 physical workspace unavailable\' : p.simulationMode === \'policy_sim\' ? \'MicroDuck runtime visual unavailable\' : p.simulationMode === \'kinematic_pose\' ? \'Unitree rig workspace unavailable\' : \'Pinned source task unavailable\');", "      this.problem(\'error\', migratedPhysical ? \'PHYSICAL_WORKSPACE\' : p.simulationMode === \'policy_sim\' ? \'MICRODUCK_RIG\' : p.simulationMode === \'kinematic_pose\' ? \'RIG_WORKSPACE\' : \'SOURCE_TASK\', String(error.message || error));\\n      this.setStatus(migratedPhysical ? `${p.shortLabel} physical workspace unavailable` : p.simulationMode === \'policy_sim\' ? \'MicroDuck runtime visual unavailable\' : p.simulationMode === \'kinematic_pose\' ? \'Unitree rig workspace unavailable\' : \'Pinned source task unavailable\');", 1], ["src/app-v2.js", "? `SO-101 uses one authoritative browser MuJoCo PhysicsSession. Rendering, live Python, WebMCP, and task evaluation consume that same state. ${scenario.limitations.join(\' \')}`", "? `${p.shortLabel} uses one authoritative browser MuJoCo PhysicsSession. Rendering, live Python, WebMCP, and task evaluation consume that same state. ${scenario.limitations.join(\' \')}`", 1], ["src/app-v2.js", "? \'Reset all files for this SO-101 physical workspace to the live async MuJoCo starter?\'", "? \'Reset all files for this physical MuJoCo workspace to the live async starter?\'", 1], ["src/app-v2.js", "if (this.isPhysicalWorkspace()) throw new Error(\'SO-101 physical workspaces execute live async Python and do not compile to replay events.\');", "if (this.isPhysicalWorkspace()) throw new Error(\'Physical MuJoCo workspaces execute live async Python and do not compile to replay events.\');", 1], ["src/app-v2.js", "$(\'stepBtn\').title = physical ? \'SO-101 physical Python uses live async execution; edit code and use Run.\' : \'Step physical action (F10)\';", "$(\'stepBtn\').title = physical ? \'Physical MuJoCo Python uses live async execution; edit code and use Run.\' : \'Step physical action (F10)\';", 1], ["src/app-v2.js", "this.setStatus(\'SO-101 live Python and MuJoCo resumed\');", "this.setStatus(`${PROFILES[this.profileId].shortLabel} live Python and MuJoCo resumed`);", 1], ["src/app-v2.js", "this.setStatus(\'SO-101 live Python and MuJoCo paused\');", "this.setStatus(`${PROFILES[this.profileId].shortLabel} live Python and MuJoCo paused`);", 1], ["src/app-v2.js", "runPhysicalSo101", "runPhysicalMujoco", 2], ["src/app-v2.js", "this.setStatus(\'Running live SO-101 physical Python against the authoritative MuJoCo session\\u2026\');", "this.setStatus(`Running live ${PROFILES[this.profileId].shortLabel} physical Python against the authoritative MuJoCo session\\u2026`);", 1], ["src/app-v2.js", "robotId: \'so101_follower\',", "robotId: this.scenario.robotId,", 1], ["src/app-v2.js", "this.setStatus(evaluation?.success ? \'Run complete \\u00b7 SO-101 physical block transfer succeeded\' : \'Run complete \\u00b7 physical task criteria not yet satisfied\');", "this.setStatus(evaluation?.success ? `Run complete \\u00b7 ${PROFILES[this.profileId].shortLabel} physical task succeeded` : \'Run complete \\u00b7 physical task criteria not yet satisfied\');", 1], ["src/app-v2.js", "this.setStatus(\'SO-101 live physical Python run failed\');", "this.setStatus(`${PROFILES[this.profileId].shortLabel} live physical Python run failed`);", 1], ["src/app-v2.js", "if (this.isPhysicalWorkspace()) { this.setStatus(\'SO-101 physical Python is live async; use Run after editing the program.\'); return false; }", "if (this.isPhysicalWorkspace()) { this.setStatus(`${PROFILES[this.profileId].shortLabel} physical Python is live async; use Run after editing the program.`); return false; }", 1], ["src/app-v2.js", "<div>SO-101 state, contacts, block motion, and task evidence are read from the authoritative MuJoCo PhysicsSession. This is simulator evidence, not hardware calibration.</div>", "<div>Physical state, contacts, object motion, and task evidence are read from the authoritative MuJoCo PhysicsSession. This is simulator evidence, not hardware calibration.</div>", 1], ["src/app-v2.js", "? \'ACTUAL MUJOCO GROUND-TRUTH STATE \\u2014 SI units/radians from the single SO-101 PhysicsSession; not commanded targets and not hardware telemetry.\'", "? \'ACTUAL MUJOCO GROUND-TRUTH STATE \\u2014 SI units/radians from the single authoritative PhysicsSession; not commanded targets and not hardware telemetry.\'", 1], ["tests/browser-smoke.spec.mjs", "    for (const [profileId, descriptors] of Object.entries(catalog.PATCH_TASKS)) {\\n      for (const descriptor of descriptors) {", "    for (const [profileId, descriptors] of Object.entries(catalog.PATCH_TASKS)) {\\n      if (profileId === \'openarm\') continue;\\n      for (const descriptor of descriptors) {", 1], ["tests/browser-smoke.spec.mjs", "    \'openarm-04-filtration-workcell\',\\n", "", 1], ["tests/browser-smoke.spec.mjs", "test(\'learner Python reaches the first physical action through the IDE Step Action path\', async ({ page }) => {", "test(\'legacy LeKiwi learner Python reaches the first action through the IDE Step Action path\', async ({ page }) => {", 1], ["tests/browser-smoke.spec.mjs", "  await expect(page.locator(\'#statusMessage\')).toContainText(\'Ready\', { timeout: 45_000 });\\n  await page.locator(\'#stepBtn\').click();\\n  await expect(page.locator(\'#statusMessage\')).toContainText(\'Stepped A01\', { timeout: 90_000 });", "  await expect(page.locator(\'#statusMessage\')).toContainText(\'Ready\', { timeout: 45_000 });\\n  await page.locator(\'#robotSelect\').selectOption(\'lekiwi\');\\n  await expect(page.locator(\'#statusMessage\')).toContainText(\'Ready\', { timeout: 45_000 });\\n  await page.locator(\'#stepBtn\').click();\\n  await expect(page.locator(\'#statusMessage\')).toContainText(\'Stepped A01\', { timeout: 90_000 });", 1], ["tests/browser-smoke.spec.mjs", "test(\'Pause holds an active source-plant run and resumes it in place\', async ({ page }) => {", "test(\'Pause holds an active LeKiwi source-plant run and resumes it in place\', async ({ page }) => {", 1], ["tests/browser-smoke.spec.mjs", "  expect(await page.locator(\'#runBtn\').evaluate((button) => button.nextElementSibling?.id)).toBe(\'pauseBtn\');\\n\\n  await page.locator(\'#runBtn\').click();", "  expect(await page.locator(\'#runBtn\').evaluate((button) => button.nextElementSibling?.id)).toBe(\'pauseBtn\');\\n  await page.locator(\'#robotSelect\').selectOption(\'lekiwi\');\\n  await expect(page.locator(\'#statusMessage\')).toContainText(\'Ready\', { timeout: 45_000 });\\n\\n  await page.locator(\'#runBtn\').click();", 1], ["tests/browser-smoke.spec.mjs", "test(\'source-plant and Unitree keep their main-thread compile/replay Run and Run-to-Cursor paths\', async ({ page }) => {", "test(\'LeKiwi source-plant and Unitree keep their main-thread compile/replay Run and Run-to-Cursor paths\', async ({ page }) => {", 1], ["tests/browser-smoke.spec.mjs", "  const sourceLine = await setFirstActionCursor();", "  await page.selectOption(\'#robotSelect\', \'lekiwi\');\\n  await expect(page.locator(\'#statusMe